DATABASE_URL="postgresql://batuhanarioz@localhost:5432/workforce_execution_platform"
REDIS_URL="redis://localhost:6379"
# Required. Generate a strong random value, e.g. `openssl rand -hex 32`.
# The API refuses to start without this — there is no insecure default.
WFP_AUTH_SECRET="replace-with-a-long-random-secret"
